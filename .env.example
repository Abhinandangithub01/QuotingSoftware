# Zoho Books API Configuration (INDIA)
# Get these from: https://api-console.zoho.in/

# Frontend variables (with VITE_ prefix)
VITE_ZOHO_CLIENT_ID=your_client_id_here
VITE_ZOHO_CLIENT_SECRET=your_client_secret_here
VITE_ZOHO_REDIRECT_URI=http://localhost:5173/zoho/callback
VITE_ZOHO_ORGANIZATION_ID=your_organization_id_here
VITE_ZOHO_API_DOMAIN=https://www.zohoapis.in

# Backend variables (without VITE_ prefix) - MUST MATCH FRONTEND
ZOHO_CLIENT_ID=your_client_id_here
ZOHO_CLIENT_SECRET=your_client_secret_here
ZOHO_REDIRECT_URI=http://localhost:5173/zoho/callback
ZOHO_API_DOMAIN=https://www.zohoapis.in

# Server Configuration
NODE_ENV=development
PORT=3001

# IMPORTANT NOTES:
# 1. REDIRECT_URI must be EXACTLY the same in:
#    - This .env file (both VITE_ and non-VITE_ versions)
#    - Zoho API Console (Authorized Redirect URIs)
#
# 2. For production, use:
#    ZOHO_REDIRECT_URI=https://your-domain.com/zoho/callback
#
# 3. Zoho API Console setup:
#    - Go to https://api-console.zoho.in/
#    - Create "Server-based Applications" client
#    - Add redirect URIs:
#      * http://localhost:5173/zoho/callback (development)
#      * https://your-domain.com/zoho/callback (production)
#    - Scopes: ZohoBooks.fullaccess.all
